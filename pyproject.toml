[project]
name = "fastapi-workshop"
version = "0.1.0"
description = "A tiny FastAPI app for open-source contribution practice"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
